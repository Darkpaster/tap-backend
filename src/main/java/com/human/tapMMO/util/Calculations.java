package com.human.tapMMO.util;

import com.human.tapMMO.model.Position;

public abstract class Calculations {
//    private static final int CHUNK_SIZE = 32;
//
//    public static boolean isUpdatable(Position target, Position target2) { //для передачи необходимых (в зоне видимости) сущностей клиенту
//        return Math.sqrt(Math.pow(target.getX() - target2.getX(), 2) + Math.pow(target.getY() - target2.getY(), 2)) < CHUNK_SIZE * 48;
//    }
}
